#include <stdio.h>
#include<math.h>

int main()
{
    int x = 9;

    // x = sqrt(x); function to find square root 
    // x = pow(x , 4); function to find power of x
    // x = round(x); function to round up to the nearest whole number
    // x = floor(x); function to round down
    // x = ceil(x); function to round up
    // x = abs(x); function to find the absolute value 
    // x = log(x); function to find the logarithm of numbers 
    // x = sin(x); function to find sin
    // x = cos(x); function to find cosine
    // x = tan(x); function to fin tangent
    printf("%d", x);
}